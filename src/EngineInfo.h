
struct WindowInfo {
    const char* title = "Note Hero";
};

struct DefaultVideoSettings {
    int windowWidth = 1280;
    int windowHeight = 720;
    int targetFPS = 60;
};

// All game settings will go here, eventually we need to setup config files to fetch from.